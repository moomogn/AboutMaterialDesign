<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arno.aboutmaterialdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tablayout.TabLayoutActivity" />
        <activity android:name=".drawerlayout.DrawerLayoutActivity" />
        <activity android:name=".textinput.TextInputActivity" />
        <activity android:name=".floating.FloatingActionButtonActivity" />
        <activity android:name=".snackbar.SnackbarActivity" />
        <activity android:name=".coordinator.CoordinatorActivity" />
        <activity android:name=".toolbar.ToolbarActivity" />
        <activity android:name=".toolbar.ToolbarTestActivity1"></activity>
    </application>

</manifest>